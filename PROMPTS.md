## Key Prompts Used

1. Initial scaffold — created all empty files
2. ingest.py — flexible CSV column matching with fallback
3. audit.py — 17 SEO detectors, single loop, rulebook aligned
4. fix.py — title generator from URL slug
5. dashboard.py — Flask dark theme, auto-refresh
6. run.py — master orchestrator